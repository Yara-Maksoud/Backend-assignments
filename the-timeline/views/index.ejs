<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Time Line</title>
</head>
<body>
    <h1>Time Line</h1>
    <div class = "content">
        <% if(data.length > 0) { %>
            <%data.forEach(user => { %>
                <div class = "post">
                    <h3><%= user.name%> <%= user.createdAt %></h3>
                    <p><%= user.message %></p>
                </div>
                
            <% }) %>
        <% } else { %>
                <p>There is no posts</p>
        <% } %>
    </div>
</body>
</html>